import { createCalendar, destroyCalendar, TimeGrid } from '@event-calendar/core'
// Import CSS if your build tool supports it
import '@event-calendar/core/index.css'

export { createCalendar, destroyCalendar, TimeGrid }
