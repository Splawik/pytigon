% extends "base.html"

%% content
    h1...Import do systemu QlikView
    form action={{request.get_full_path}},,,method=post,,,enctype=multipart/form-data
        % csrf_token
        table
            {{form}}
        input type=submit,,,value=Wczytaj dokument
    br