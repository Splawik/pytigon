!!!APPS=[{% for app in app_names %}'{{app}}',{% endfor %}{% for app in appset.get_ext_apps%}'{{app|safe }}',{% endfor %}]
PUBLIC = {% if appset.public %}True{% else %}False{% endif %}
MAIN_APP_PACK = {% if appset.main %}True{% else %}False{% endif %}
