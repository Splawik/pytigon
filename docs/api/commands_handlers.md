# commands.handlers – Command Handlers

Implementations of command handlers for each CLI command pattern:
manage, run, runserver, python, pip, init, tools, and default.

::: pytigon.commands.handlers
    options:
      show_submodules: true
      members: true
