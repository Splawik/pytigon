# commands.dispatcher – CommandDispatcher

Routes commands to appropriate handlers using the `CommandRegistry`.
Provides centralized error handling and logging. The dispatcher is
instantiated per CLI invocation and is not shared across calls.

::: pytigon.commands.dispatcher
    options:
      show_submodules: false
      members: true
