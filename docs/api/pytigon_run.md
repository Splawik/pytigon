# pytigon_run – CLI Runner and Command Dispatcher

Main entry point for Pytigon CLI commands: `manage_*`, `runserver_*`, `run_*`,
`python_*`, `pip_*`, `init_*`, and tool commands (zig, nim, nimble).

::: pytigon.pytigon_run
    options:
      show_submodules: false
      members: true
