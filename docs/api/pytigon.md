# pytigon – Root Package

Package init, version info, and metadata. Sets up the `START_PATH` and
`XKB_CONFIG_ROOT` environment variables and registers the `ptig` console
entry point.

::: pytigon
    options:
      show_submodules: false
      members: true
