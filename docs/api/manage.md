# manage – Django Management Bridge

Django `manage.py` entry point configured to work within pytigon's
path structure and environment.

::: pytigon.manage
    options:
      show_submodules: false
      members: true
