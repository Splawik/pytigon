% load exfiltry
% load exsyntax

%% all
    %% js_standard

    %% widget_content
        {% jscript_link 'ckeditor/ckeditor.js' %}
        <textarea class="ckeditor" id='{{id}}' name='{{id}}' style='width: {{width}}; height: {{height}}'>Hello world!, {{data}}</textarea>

    %% js

    %% js2
        script {:}
            CKEDITOR.replace( '{{id}}' );
